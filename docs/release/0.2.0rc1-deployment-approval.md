# Dockyard 0.2.0rc1 deployment approval packet

Status: LOCAL RC PASS - external decisions required
This packet requests decisions; it authorises nothing by itself.

## Proven locally

- full Dockyard workspace verify passes;
- native Hermes Project/Kanban regression suite passes;
- dashboard tests and deterministic build pass;
- Desktop behavioural harness passes 20/20;
- runtime readiness returns HTTP 200;
- operational export/restore and migration rollback pass on disposable state;
- static security and diff checks pass.
- local Dockyard and host commits exist;
- immutable fresh clones pass all Python, host, dashboard and Desktop gates;
- committed source and built-plugin hashes are recorded;
- direct-only owner instruction is recorded as the independent-review waiver.

## Not yet done

- no Git tag or publish;
- no installed candidate byte comparison;
- no live database backup/restore rehearsal;
- no live Desktop install/reload;
- no scheduler activation;
- no production legacy migration;


## Approval gates

Each gate is separate. Approval of one does not approve the next.

### A. Local commit

State: complete.

Effect: create path-scoped commits in the two candidate worktrees.
Recommendation: approve after final direct verification.
Does not push, install or activate.

### B. Fresh-clone verification

State: complete.

Effect: clone/check out the committed candidates into disposable paths, install
frozen dependencies, run complete gates, and record source/dist hashes.
Recommendation: approve with A or immediately after A.

### C. Independent review or waiver

State: complete by owner direct-only waiver.

Effect: either permit one read-only independent reviewer of the exact committed
candidate, or record an explicit owner waiver because implementation and review
must remain direct-only.
The waiver applies to this RC only and does not authorise external actions.

### D. Publish

Effect: push branches/open PRs or publish package/plugin artefacts.
Recommendation: hold until A-C pass and destination is named.

### E. Local Desktop pilot

Effect: back up current state, install immutable plugin bytes, reload/restart the
approved Desktop runtime, run smoke checks and observe a bounded pilot.
Recommendation: approve only after D if publishing is required, otherwise after
A-C with exact local paths and rollback owner recorded.

### F. Production legacy migration

Effect: migrate approved legacy work into canonical Hermes Project/Kanban.
Recommendation: hold. First inventory live source/writers and complete the
marker-owned isolated rehearsal against a fresh copy.

### G. Scheduler or Discord activation

Effect: enable scheduled workflow/cycle execution or real channel delivery.
Recommendation: hold until manual Desktop pilot evidence exists and channel/
error ownership is agreed.

## Recommended approval sequence

1. Review the immutable RC evidence.
2. Approve D only if publication is wanted.
3. `APPROVE E` separately for a local Desktop pilot.
4. Consider F/G only after pilot soak.

## Required owner decisions

- Pilot the current RC, or wait for the full Priority-1 product loop?
- Keep autonomy levels 4-5 disabled? Recommended: yes.
- Pilot surface: local Desktop only, or Desktop plus one Discord channel?
  Recommended: local Desktop only.
- Is there legacy production data requiring migration? If yes, identify its
  owner and current writer processes before any access.

## Current verdict

Engineering: PASS.
Local RC gate: PASS.
Publish authorisation: HOLD.
Deployment authorisation: HOLD.
Production migration authorisation: HOLD.
